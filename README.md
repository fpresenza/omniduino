# odniduino

--> Guardar la carpeta arduino/omniduino con los .ino, .cpp y .h dentro.

--> El IDE de Arduino, obliga a tener todos los archivos fuente en la misma carpeta
y que ésta tenga el mismo nombre que el .ino

-->En otra carpeta guardar los scripts python y la carpeta /ensayos

-->En otra carpeta guardar los scripts matlab y simulink. Agregar dicha ruta
al "path" de Matlab.